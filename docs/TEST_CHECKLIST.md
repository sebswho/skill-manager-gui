# Dev 分支测试检查清单

**分支**: `dev` (已推送至远程)  
**主要变更**: Select-and-Show UI 重新设计  
**状态**: 待测试验证

---

## 快速验证

### 1. 构建验证
```bash
cd /Users/swamartin/workspace/projects/skill-manager-gui
bun run build
```
**预期**: ✅ 构建成功，无 TypeScript 错误

### 2. 单元测试
```bash
bunx vitest run
```
**预期**: ✅ 20 个测试全部通过

### 3. E2E 测试
```bash
bun run test:e2e
```
**预期**: ✅ 19 个测试全部通过

---

## 功能测试清单

### UI 布局
- [ ] 左侧显示"我的技能库"侧边栏
- [ ] 侧边栏包含"本地"、"已安装"、"发现技能"分类
- [ ] 右侧显示空状态（首次打开时）
- [ ] 搜索框在侧边栏顶部

### 技能选择
- [ ] 点击技能项，右侧显示详情面板
- [ ] 选中的技能高亮显示（左侧绿色边框）
- [ ] 技能详情显示名称、来源、大小、日期
- [ ] 长描述支持展开/收起

### Agent 卡片
- [ ] 显示 Agent 卡片网格（图标、名称、状态）
- [ ] 点击卡片切换选择状态
- [ ] 选中卡片显示绿色边框和勾选图标
- [ ] 状态文字实时更新（已安装/未安装/将安装/将卸载）

### 同步操作
- [ ] 无变更时"一键同步"按钮禁用
- [ ] 选择 Agent 后按钮启用
- [ ] 显示变更摘要（将安装/将卸载数量）
- [ ] 点击同步执行操作
- [ ] 同步成功后状态更新

### 全选/取消全选
- [ ] "全选"按钮选中所有 Agent
- [ ] "取消全选"按钮清空所有选择

### 搜索过滤
- [ ] 搜索框输入实时过滤技能列表
- [ ] 匹配的技能显示，不匹配的隐藏

### 设置面板
- [ ] 点击 Settings 按钮打开设置面板
- [ ] 设置面板包含主题、语言、Hub 路径、Agents

---

## 回归测试

### 主题切换
- [ ] 亮色/暗色主题切换正常
- [ ] 主题偏好持久化

### 语言切换
- [ ] 中英文切换正常
- [ ] 语言偏好持久化

### Agent 管理
- [ ] 添加新 Agent 正常工作
- [ ] 删除 Agent 正常工作
- [ ] Agent 列表自动发现

---

## 通过标准

- [ ] 所有功能测试项通过
- [ ] 单元测试 20/20 通过
- [ ] E2E 测试 19/19 通过
- [ ] 手动测试无明显 Bug
- [ ] 代码审查通过（如适用）

---

## 已知限制

1. **移除功能**: 旧版批量操作界面已移除
2. **新术语**: "Sync" → "Install to"，"Central Hub" → "My Skill Library"
3. **Agent 限制**: 当前版本需要手动配置 Agent 路径

---

## 合并到 Main 前准备

1. 完成上述所有测试项
2. 更新 CHANGELOG.md（如需要）
3. 更新版本号（如需要）
4. 确保 CI/CD 通过
5. 可选：创建 Release Notes

---

## 命令速查

```bash
# 运行开发模式
bun run tauri:dev

# 构建生产版本
bun run bundle:mac  # macOS
bun run tauri:build # 其他平台

# 运行所有测试
bunx vitest run          # 单元测试
bun run test:e2e         # E2E 测试

# 查看 Git 状态
git log main..dev --oneline  # dev 超前 main 的提交
git diff main..dev --stat    # 文件变更统计
```

---

**最后更新**: 2026-03-19  
**负责人**: dev 分支维护者
